<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="btnBotonNumerico" parent="@android:style/Widget.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/colorNegro</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">30dp</item>
        <item name="android:layout_marginLeft">-8dp</item>
        <item name="android:layout_marginBottom">-10dp</item>
    </style>

    <style name="btnBotonOperacion" parent="@android:style/Widget.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorBlanco</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">40dp</item>
        <item name="android:background">@color/colorAccent</item>
    </style>
    <style name="btnBotonAbajo" parent="@android:style/Widget.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textColor">@color/colorNegro</item>
        <item name="android:textSize">30dp</item>
        <item name="android:layout_marginLeft">-8dp</item>
        <item name="android:layout_marginBottom">-10dp</item>
    </style>

</resources>
